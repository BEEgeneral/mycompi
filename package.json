{
  "name": "mycompi",
  "version": "1.0.0",
  "description": "MyCompi - Plataforma de profesionales agénticos",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "build:admin": "cd admin-panel && npm install && npm run build",
    "build:chat": "cd chat-panel && npm install && npm run build",
    "build:landing": "cd landing && npm install && npm run build",
    "build": "npm run build:landing && npm run build:admin && npm run build:chat",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@prisma/client": "^6.19.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "express-rate-limit": "^8.3.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3",
    "prisma": "^6.19.2",
    "zod": "^4.3.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
