# 2026-04-05 — Enzo migration notes

## Qué hice

### Landing → Cloudflare Pages ✅

1. **Revisé el código**: `/data/.openclaw/workspace/mycompi/landing/` tiene un proyecto Vite+React bien configurado.
2. **Build local verificado**: `npm run build` funciona correctamente.
   - Output en `../public/` (118KB gzip JS, 8KB gzip CSS)
   - 58 módulos transformados en ~2s
3. **GitHub Actions workflow creado**: `landing/.github/workflows/deploy.yml`
   - Se dispara en push a `main` cuando cambia `landing/**`
   - Usa `cloudflare/pages-action@v1`
   - Build output sube a `public/` (el mismo directorio que usa el Express en producción)

### Worker API — Estado actual ✅

Revisé `workers/`:
- `src/index.ts` tiene un Hono app completo con todos los routers (auth, agentes, trabajos, approvals, audit, pagos, notificaciones, admin-metrics, stripe-webhook, stripe-config, chat, debug)
- Usa Durable Objects para chat (`src/durable-objects/chat.durable.ts`)
- Prisma schema en `workers/prisma/schema.prisma`
- Wrangler config con `compatibility_date: "2024-01-01"` y cuenta de Cloudflare asignada
- GitHub Actions workflow ya existe en `workers/.github/workflows/deploy.yml` — deploya a Workers con todos los secrets
- Wrangler no está autenticado localmente (requiere `wrangler login`), pero el pipeline de GitHub Actions sí tiene `CF_API_TOKEN`

## Problemas encontrados

1. **Landing no tenía GitHub Actions** — lo acabo de crear
2. **Wrangler local no autenticado** — no critical porque el deploy va por GitHub Actions
3. **El `public/` directory es compartido** — landing build genera estáticos ahí, y Express sirve desde ahí en Render. En Cloudflare Pages no hay servidor Express, así que el `public/` es el root del deploy. Eso está bien.

## Qué queda pendiente

### Landing (prioridad alta)
- ⚠️ **Crear secrets en GitHub repo**: `CF_PAGES_API_TOKEN` y `CF_ACCOUNT_ID`
- ⚠️ **Configurar Cloudflare Pages** desde el dashboard para el proyecto `mycompi-landing` (conectar al repo GitHub `BEEgeneral/mycompi`)
- ⚠️ **Decidir dominio**: ¿mycompi.com apunta a Cloudflare Pages o sigue en otra parte? ¿Redirect desde onrender.com?

### Worker API
- Parece funcionalmente completo según el commit `15354ab` (chat + Durable Objects)
- Necesita secrets en GitHub Actions (ya están listados en el workflow, solo falta configurar)
- Posible problema: el `wrangler.toml` tiene `account_id` hardcodeado — verificar que esté correcto

## Decisiones de arquitectura

1. **Landing en Cloudflare Pages** — genera estáticos en `public/`, se sirve directamente
2. **Worker API en Cloudflare Workers** — ya tiene pipeline de GitHub Actions funcionando
3. **El `public/` es el output de landing y el root de Cloudflare Pages** — compatible con el setup actual
4. **No toqué el Worker** — Alberto quería prioridad en Pages y el Worker ya tiene un workflow funcional en GitHub Actions (aunque pueda necesitar ajustes)